# 💾 Advanced SQL Case Studies

### 🚀 Overview
A collection of complex SQL queries solving realistic business problems. This repository demonstrates proficiency in **data manipulation**, **reporting**, and **performance optimization** using PostgreSQL/standard SQL syntax.

### 🧠 Key Concepts Demonstrated
* **Window Functions:** `RANK()`, `DENSE_RANK()`, `ROW_NUMBER()`, and Moving Averages (`SUM() OVER`).
* **Complex Joins:** `SELF JOIN` for hierarchy analysis and Multi-table `INNER/LEFT JOINs`.
* **Advanced Aggregation:** `GROUP BY` with `HAVING` clauses for filtered reporting.
* **CTEs (Common Table Expressions):** Writing modular, readable queries for multi-step logic.

### 🔍 Highlighted Solutions

| Problem | Type | SQL Feature |
| :--- | :--- | :--- |
| **Running Total Sales** | Financial Reporting | Window Functions (`SUM OVER`) |
| **Employee-Manager Hierarchy** | Org Structure | `SELF JOIN` |
| **Duplicate Removal** | Data Cleaning | `ROW_NUMBER()` & CTEs |
| **Top 5 Profitable Items** | Sales Analysis | `GROUP BY`, `ORDER BY` |

### 💻 Usage
These queries are designed to be run against standard e-commerce or HR database schemas.
